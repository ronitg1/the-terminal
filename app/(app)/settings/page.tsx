import { PhaseStub } from "@/components/shell/PhaseStub";
export default function Page() {
  return <PhaseStub title="Settings" description="Manage tickers, peer groups, macro search terms, mega cap list, agent schedule, weekly email, notifications, export/clear data. Built in Phase 2." />;
}
